{{ brand_name or "心流（Flow）" }} — 邮箱绑定验证码

你好，{{ username }}：

正在为你的账号绑定邮箱 {{ email }}，验证码如下：

    {{ code }}

注意：
- 验证码 {{ ttl_minutes }} 分钟内有效，只能使用一次。
- 如果不是你本人发起的操作，请忽略本邮件。

—— {{ brand_name or "心流" }} 团队
